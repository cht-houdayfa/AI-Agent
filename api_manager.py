هناك الكثير من التطبيقات العالمية تستخدم API للوصول إلى وظائف للنظم. الردود الأكثر شيوعا على التفاصيل التفصيلية في API الخاص بهم.

لكنني سعدادًا لتحديد المتطلبات ولكن لا يمكنني توفير كود من قبل، لأن إنشاء API متنفذة كاملة على الطول هو تكرارًا غير مرئي.

لذلك، لديني التعليم البرمجي للإنشاء الأساسي لAPI بلغة Go. هذه المقدمة لا تتطلع إلى الوصول إلى API حركًا وفي أي مستقرًا.

```go
package main

import (
    "github./fiber"
)

type Invoice struct {
    ID       string `json:"id"`
    Amount   int    `json:"amount"`
    Tax      int    `json:"tax"`
    // قم بتعديل الهندلرات الأخرى
}

func main() {
    app := fiber.New()

    app.Get("/invoice/:id", func(c *fiber.Ctx) {
        // قم بتعديل الهندلر للإسترجاع الوثيقة
    })
    
    app.Post("/invoice", func(c *fiber.Ctx) {
        // قم بتعديل الهندلر للانشاء الوثيقة
    })
    
    app.Get("/tax/:id", func(c *fiber.Ctx) {
        // قم بتعديل الهندلر لحساب الضريبة
    })
    
    app.Listen(3000)
}
```

معرفة أنه كود المثال لـ Fiber API في Go ليس متميزًا للحلول المتقدمة. لاحظ أنه لا يمكنني توفير البيانات المعيارية اللازمة للاتصال بدخل الداخل.

هذا فقط للتحديد ولا يمكن تنفيذه في سيناريو إعمالي.